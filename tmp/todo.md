# Tonnel Domain AI Search and Crawl Improvement TODO

This list targets AI search and crawler visibility for the `tonnel.io` domain and its public subdomains, including `airdrop.tonnel.io`, `www.tonnel.io`, and future `*.tonnel.io` hosts. It excludes items that are already implemented in this repository, such as the FAQ UI, `/llms.txt`, sitemap, AI crawler allow rules, canonical metadata, Open Graph/Twitter metadata, baseline JSON-LD, README overview, and landing-page participation content.

## TODO

1. Verify crawler access in logs.
   - Check whether `GPTBot`, `OAI-SearchBot`, `ClaudeBot`, `PerplexityBot`, `CCBot`, and search crawlers can fetch public `tonnel.io` and `*.tonnel.io` pages.
   - Confirm Vercel, DNS, or bot protection layers do not block allowed crawlers.
